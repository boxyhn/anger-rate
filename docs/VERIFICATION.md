# 검증 결과 / 2026-09-05

- `swift test`: 45개 실행, 실패0, 기본 비활성 live CLI1개 skip.
- `ANGER_RATE_LIVE_CLI=codex swift test --filter CalibrationTests/testLive`: 로그인된 Codex0.148 실호출 성공. 합성 대화만 사용.
- Claude2.1.212 실호출: 이 Mac의 OAuth 만료로 실패. 재로그인이 필요하다. 파싱/취소/타임아웃은 합성 프로세스 테스트 통과.
- `swift build -c release` 및 `scripts/package.sh`: 성공. 서명 검증 통과(ad-hoc), arm64 .app 및 .dmg 생성.
- 네이티브 패널을 앱 자체 NSHostingView로 렌더링하고 시각 확인: `panel-preview.png` (합성 데이터).
- 실제 로컬 세션 `--scan-check`: 4,096파일, 시작시 새 이벤트0, 과거 샘플34개, 약1.96초. 이전 구현28.22초 병목 수정. 기존 로그에 쓰기 없음.
- 두 가상 기기: 동일 이벤트 중복 제거, 프로필/반감기 공유, 손상·미래·오래된 데이터, 일시적 장애/restart 캐시 테스트 통과.
- 알림: 실제 이벤트 시점의100 peak를3초 뒤 poll에서도 감지, 한번 발송 gate,50 이하 재무장, 과거/replay 억제, 발신 기기 구분 테스트 통과.

## 남은 실제 환경 검증
- Claude 재로그인 후 실제 분석 성공.
- 두 물리 Mac에 앱 설치 및 동일 동기화 폴더 선택 후 실제 전파 지연 확인.
- 사용자가 macOS 알림 권한을 허용한 뒤 실제 시스템 배너 수신 확인.
- Intel Mac/macOS13 실기기 확인, Developer ID 서명·Apple 공증. 현재 로컬 빌드는 arm64/ad-hoc이며 공개 게시하지 않았다.

## 지표/수집 한계
실제 감정 진단이 아니라 표현 휴리스틱이다. 첫 분석은 최근40파일의 tail을 읽은 최대400메시지 중48KiB 이내 표본이다. 모든 과거 대화의 완전한 분석은 아니다. 라이브 감지는 최근수정4096파일까지만 관리하며 앱이 꺼져 있거나 일시정지한 동안의 메시지를 소급 알림하지 않는다. 파일 목록 캐시 때문에 새 세션 발견은 최대30초 지연될 수 있다. 여러 기기 동시 입력의 알림은 전역 exactly-once 보장이 없다.
