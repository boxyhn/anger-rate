# 검증 결과 / 2026-09-05

## 현재 버전
- `swift test`: 65개 실행, 64개 통과, 실패0, 실 CLI 테스트1개는 명시적으로 실행할 때만 활성화.
- `scripts/package.sh`: release 빌드 및 ad-hoc 서명 검증 성공. 설치된 앱과 DMG 업데이트.
- 공통 한·영 욕설90개: 개인 기준 없이 감지, 개인 규칙으로 기본 사전을 비활성화할 수 없음, 중복·영문 단어 경계·중립적인 한국어 사용·인용/코드 제외 테스트.
- 전체 이력: 기존 파일 수/tail 제한 없이 전체 JSONL 스트리밍. 165개 이상 파일, 앞부분 메시지, 아카이브, 중복, 손상/초대형 행, 파일 사라짐, 취소 테스트.
- 실제 로컬 현재·아카이브 전체 점검 실행 완료. 읽기 실패 없이 완료했고, 초대형 행 제외 건수는 앱 데이터 폴더의 audit-summary.json에 기록했다. 원문이나 개인별 분석 결과는 저장소에 넣지 않는다.
- 개인화: 기본 욕설을 중복 제안하지 않고 개인 분노 조짐만 반환, 전체 집계 맥락 전달, 개인 신호0개 정상 처리 테스트.
- 점수: 순간100 도달을 poll 이후에도 감지, 1회 알림 gate/50 이하 재무장/과거 재생 방지/발신기기 구분.
- 동기화: 두 가상 Mac 중복 제거/프로필·반감기 공유/손상·미래 기록 검증/일시적 장애 및 재시작 캐시.
- CLI 프로세스: 정상 종료/타임아웃/취소/출력 제한/도구 이벤트 거부 테스트. 기존 Codex 실계정 합성 분석 성공. Claude 실계정 분석은 아직 검증하지 않았다.

- 온도 표시:36.5–100°C/97.7–212°F, 중간값·범위 제한·상한 직전 반올림 테스트. 단위는 화면 전체와 알림에 적용하고 내부 점수/알림 게이트는 유지.
- 아이콘:1024px 원본 → 표준 iconset → ICNS 생성, 번들 Info.plist 연결 및 서명 검증. 실제 패널은 합성 데이터로 재렌더링.
- README 영문/한국어 링크와 로컬 이미지 경로 확인, 셸 스크립트 문법 검사 통과.
- GitHub Actions 설정은 추가했지만 원격 저장소가 없어 호스팅된 CI 실행은 미검증.

## 실제 환경에서 남은 검증
- Claude의 로그인된 계정으로 실제 분석.
- 두 물리 Mac에서 같은 동기화 폴더를 선택한 뒤 전파 지연 확인.
- macOS 알림 권한 허용 후 실제 시스템 배너 확인.
- Intel Mac/macOS13 실기기, Developer ID 서명·Apple 공증. 현재 arm64 로컬 빌드이며 공개 게시하지 않았다.

## 분석 범위
초기 진단은 접근 가능한 로컬 파일 전체를 읽지만4MiB 초과 행은 명시적으로 제외한다. 모든 원문을 AI에 전송하지 않는다. 전체 통계와 시기·세션별 대표 문맥 최대600개를 선별한 뒤 CLI에는 최대400개/원문48KiB와 통계를 전달한다. 원격 또는 서버에만 있는 계정 대화는 자동으로 내려받지 않는다. 초기 진단의 전체 파일 점검과 라이브 감지의 최근4096파일 제한을 구분한다.
